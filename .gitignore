venv/
.env
.env.*
!.env.example
__pycache__/
*.pyc
*.egg-info/
.coverage
.pytest_cache/
.claude/
# Top-level data/db/results trees only — anchored with a leading slash so
# nested directories like src/data/ aren't silently swept up. Without the
# anchor, src/data/*.py was untracked from the project's start.
/data/
/db/
/results/
strategies/generated/
